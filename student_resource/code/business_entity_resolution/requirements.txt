# Core data processing
pandas==3.0.6
numpy==2.5.3
pyarrow==25.0.1

# String similarity / normalization (audit + experiments pipeline)
rapidfuzz==3.14.6
regex==2026.9.10

# GPU / embeddings (LaBSE stretch-goal blocking; optional, not on required path)
# Install torch separately for your CUDA version, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
sentence-transformers==6.1.0
transformers==5.17.0
huggingface_hub==1.33.0

# Stage 4: pairwise classifier + calibration + GroupKFold
lightgbm==4.7.0
scikit-learn==1.9.1

# Sparse matrix retrieval (used by teammate's blocking notebooks for
# char-ngram TF-IDF + cosine blocking, src/blocking_eval_approach.ipynb)
# scikit-learn (already listed above) covers TfidfVectorizer/cosine_similarity
# sparse_dot_topn
