# === Dataset (large, provided separately by the challenge) ===
student_resource/dataset/

# === Local caches (regenerable from the dataset, large) ===
student_resource/audit/cache/
audit/cache/

# === Transient run logs from local experimentation ===
student_resource/*.log
*.log

# === Python ===
__pycache__/
*.pyc
*.pyo
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/

# === macOS zip artifacts ===
__MACOSX/
.DS_Store

# === OS / editor ===
Thumbs.db
.vscode/
.idea/

# === Claude Code session/runtime state (not project content) ===
.claude/

# === Empty stray directory from an early misrun (kept out just in case it repopulates) ===
/audit/


blocking_results/

# === Large regenerable intermediate pipeline artifacts (Parquet feature/
# probability files, the raw finalized-sample TSV) -- keep the small final
# output/ + JSON reports as evidence, regenerate the rest from the scripts ===
student_resource/code/business_entity_resolution/pipeline_output/*.parquet
student_resource/code/business_entity_resolution/pipeline_output/candidate_pairs_sample.tsv